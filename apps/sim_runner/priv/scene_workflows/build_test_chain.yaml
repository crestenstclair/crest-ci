name: Build and Test
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v4
      - run: make build
  test:
    needs: build
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v4
      - run: make test
